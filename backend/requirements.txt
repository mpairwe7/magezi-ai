# Magezi — A-Level STEM Tutor Dependencies
# Python 3.11+

# API framework
fastapi==0.115.12
uvicorn[standard]==0.34.2
pydantic==2.11.3

# Claude API
anthropic==0.52.0

# Vector store
qdrant-client==1.14.2

# Embeddings + reranking — CPU-only deployment
# For deterministic CPU install: pip install --index-url https://download.pytorch.org/whl/cpu torch
# Then: pip install -r requirements.txt (resolver respects pre-installed torch)
sentence-transformers==4.1.0
torch>=2.5.0

# Redis cache
redis==5.3.0

# Auth
PyJWT[crypto]>=2.8.0
bcrypt>=4.1.0

# Utilities
python-dotenv==1.1.0
python-multipart==0.0.20

# Voice — Silero VAD neural inference (CPU-only, ~40MB)
onnxruntime>=1.17.0,<2
